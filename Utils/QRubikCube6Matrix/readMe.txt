This is the wrapper for RubikCube6Matrix for QT.

It is defined as a QObject as base class,
then it is derived as template class to be compatible with RubikCube6Matrix template format.

Main Role of class: to provide signal/slot functionality from QT to every move on a Rubik cube.
